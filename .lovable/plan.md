Сделаю размеры надписей в шапке между предыдущими и текущими значениями, а кнопку Sign Up увеличу, но не так сильно.

### Что изменится

**Файл: `src/components/landing/Header.tsx`**

- Навигационные ссылки: уменьшить с `text-[17px]` до `text-[14px]`.
- Кнопка **Log in**: уменьшить с `text-[17px]` до `text-[14px]`.
- Селектор языка: уменьшить с `text-[18px] h-10 px-3` до `text-[14px] h-9 px-2.5`.
- Кнопка **Sign up**: заменить `pill pill-primary text-[20px]` на `pill pill-primary text-[15px] px-6 py-2.5` — надпись и сама кнопка станут крупнее базового `.pill`, но не огромными.
- Мобильное меню: ссылки уменьшить с `text-[20px]` до `text-[16px]`, Sign up — до `text-[16px] px-5 py-2.5`, Log in — до `text-[16px]`, селектор языка — до `text-[14px] h-9 px-3`.

**Файл: `src/components/LanguageSelector.tsx`**
- Никаких изменений не требуется: проп `className` уже поддерживается, размеры будут переданы из `Header.tsx`.

### Проверка

- Соберу проект (`tsgo`) и проверю превью, чтобы шапка выглядела сбалансированно.

### Примечание

Если после применения `14px` для ссылок и `15px` для Sign up захочешь ещё подкрутить, скажи — подберу точнее.